# Archive

Point-in-time handover and campaign documents, kept for reference. **They are not maintained and
may contradict the current system** — in particular, several describe the Marketa blog pipeline as
running from this repo (it now runs from `marketa-monorepo`) and the older shared-password portal
login (now Supabase + Google SSO).

For current architecture, always read [`../architecture.md`](../architecture.md).
