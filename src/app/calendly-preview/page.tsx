// TEMPORARY preview — renders the real CalendlySection (live Calendly API) on a
// short page for easy QA/screenshots. DELETE after review.

import CalendlySection from "@/components/sections/CalendlySection"

export default function CalendlyPreviewPage() {
  return <CalendlySection subheading="Pick a time that works for you — book instantly, no back-and-forth." />
}
